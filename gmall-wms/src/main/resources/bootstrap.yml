spring:
  application:
    name: wms-service # 微服务名称
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 # nacos配置中心地址
        namespace: 6606a2ac-3df6-4a48-9516-375468cca46b # nacos名称空间ID
        group: dev # 分组
        file-extension: yml # 文件扩展名，默认为properties