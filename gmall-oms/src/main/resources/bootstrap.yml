spring:
  application:
    name: oms-service # 微服务名称
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 # nacos配置中心地址
        namespace: b5ba51ac-1f6c-4571-a97c-c298e9cb4033 # nacos名称空间ID
        group: dev # 分组
        file-extension: yml # 文件扩展名，默认为properties